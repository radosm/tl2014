/*En este caso chequeamos que el plot tome los parametros correctos, es decir, dos funciones que
dependan solo de la variable declarada en el for*/
function id(x) {
    return x
}

function sum(x,y) {
    z = x+y
    return z
}

plot (id(x), sum(x,y)) for x=0..1..10
