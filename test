function pot(b, x) {
	y = 1
    while x > 0 {
        x = x - 1
		y = b * y
    }
    return y
}

function factorial(n) {
     if n == 1
	 then return 1
     else return n * factorial(n - 1)
}

function id(x) {
   x = x - 1
   x = x + 1
   return x
}

plot (pot(x,x),id(x+1)) for x=1..1..10
